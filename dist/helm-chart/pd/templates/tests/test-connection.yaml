apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pd.fullname" . }}-test-connection"
  labels:
    {{- include "pd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
