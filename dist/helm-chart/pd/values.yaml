pd:
  name: pd
  count: 3
  image:
    registry: docker.io
    repository: elasticstream/pd
    tag: nightly

  # ports:
  #   - name: server
  #     port: 10911
  #   - name: metrics
  #     port: 9898

  configmap_volumes:
    range_server:
      mountPath: /etc/range-server/range-server.yaml
      subPath: range-server.yaml
    range_server_log:
      mountPath: /etc/range-server/range-server-log.yaml
      subPath: range-server-log.yaml

  range_server_config: |-
