apiVersion: v1
kind: Service
metadata:
  name: {{ include "rs.fullname" . }}
  labels:
    {{- include "rs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rs.selectorLabels" . | nindent 4 }}
