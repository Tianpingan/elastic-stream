apiVersion: v1
kind: ConfigMap
matadata:
  name: {{ .Values.range-server.name }}-config
data:
  range-server.yaml:
  {{ .Values.range-server-config | indent 4 | trim }}
  range-server-log.yaml:
  {{ .Values.range-server-log-config | indent 4 | trim }}
