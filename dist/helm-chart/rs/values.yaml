range-server:
  name: range-server
  count: 1
  imagePrefix:
  configmapVolumes:
    - name: backend-config
      mountPath: /config/application.yaml
      subPath: application.yaml
  service:
    name: svc-backend
    ports:
      - name: tcp1
        port: 80
        targetPort: 8081
