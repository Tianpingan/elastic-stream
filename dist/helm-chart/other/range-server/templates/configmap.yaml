apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "range-server.fullname" . }}-cfg
  labels:
    {{- include "range-server.labels" . | nindent 4 }}
data:
  range-server.yaml: |-
{{ .Values.config.range_server_config | indent 4 }}
  range-server-log.yaml: |-
{{ .Values.config.range_server_log_config | indent 4}}
